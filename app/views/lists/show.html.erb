<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @list.name %>
</p>

<p>
  <strong>User:</strong>
  <%= @list.user.username %>
</p>

<p>
  <strong>Purchases:</strong>
  <ul>
    <% @list.purchases.each do |p| %>
      <li><%= p.product.name %>, <%= p.amount %></li>
    <% end %>
  </ul>
</p>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
